services:
  - type: web
    name: predikta
    env: python
    region: oregon
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PREDIKTA_AUTOUPDATE_SEC
        value: "1800"
      - key: PREDIKTA_AUTOUPDATE_DELAY_SEC
        value: "120"
    autoDeploy: true
